Module.doNotCaptureKeyboard = true; // Only works with SDL1 I believe.
// Module.preRun.push(() => { // works, but seems to override some other settings in the process...
//   ENV.SDL_EMSCRIPTEN_KEYBOARD_ELEMENT = "#canvas";
// });
